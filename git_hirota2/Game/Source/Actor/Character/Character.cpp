#include "stdafx.h"
#include "Character.h"

namespace nsApp{
    namespace nsActor {
        Character::Character()
        {
        }

        Character::~Character()
        {
        }

        bool Character::Start()
        {
            return true;
        }

        void Character::Update()
        {
        }

        void Character::Render(RenderContext& rc)
        {
        }
    }
}
